node_modules
.netlify/
.DS_Store
*.log

# Xcode resolves these on first open, per machine
ios/App/App.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/

# throwaway probes and screenshots a review run leaves in the root. Named, so
# nothing in the repository is invisible to `git status` by accident.
/.ux-*.mjs
/shot-*.png
